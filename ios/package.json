{
  "name": "sprintgpt-ios",
  "version": "1.6.0",
  "private": true,
  "description": "iOS (Capacitor) wrapper for SprintGPT. Loads the hosted SprintGPT server in a native WKWebView.",
  "scripts": {
    "add:ios": "cap add ios",
    "sync": "cap sync ios",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
